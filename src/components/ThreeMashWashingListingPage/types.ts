// This file is auto-generated — do not edit manually.
import type { IkasProductList } from "@ikas/bp-storefront";

export interface Props {
  /** Canlı ürün listesi */
  productList?: IkasProductList;
  /** Duyuru bandı sol vurgulu metin */
  eyebrowText?: string;
  /** Duyuru bandı açıklama */
  announcementText?: string;
  /** Duyuru bandı buton metni */
  announcementCtaText?: string;
  /** Duyuru buton linki */
  announcementHref?: string;
  /** Hero ana başlık ilk kısım */
  heroTitlePrefix?: string;
  /** Hero vurgulu eğik kelime */
  heroTitleEmphasis?: string;
  /** Hero ana başlık son kısım */
  heroTitleSuffix?: string;
  /** Hero zengin metin açıklaması */
  heroDescriptionHtml?: string;
  /** Hero ana buton metni */
  primaryButtonText?: string;
  /** Hero ana buton linki */
  primaryButtonHref?: string;
  /** Hero ikinci buton metni */
  secondaryButtonText?: string;
  /** Hero ikinci buton linki */
  secondaryButtonHref?: string;
  /** 1. metrik başlığı */
  metric1Value?: string;
  /** 1. metrik vurgulu kelime */
  metric1Emphasis?: string;
  /** 1. metrik alt açıklaması */
  metric1Label?: string;
  /** 2. metrik başlığı */
  metric2Value?: string;
  /** 2. metrik vurgulu kelime */
  metric2Emphasis?: string;
  /** 2. metrik alt açıklaması */
  metric2Label?: string;
  /** 3. metrik başlığı */
  metric3Value?: string;
  /** 3. metrik vurgulu kelime */
  metric3Emphasis?: string;
  /** 3. metrik alt açıklaması */
  metric3Label?: string;
  /** 4. metrik başlığı */
  metric4Value?: string;
  /** 4. metrik vurgulu kelime */
  metric4Emphasis?: string;
  /** 4. metrik alt açıklaması */
  metric4Label?: string;
  /** Bölüm numarası (01) */
  selectorNumber?: string;
  /** Bölüm etiketi (YIKAMA CİHAZLARI) */
  selectorLabel?: string;
  /** Seçici başlık ilk kısım */
  selectorTitlePrefix?: string;
  /** Seçici başlık vurgulu kısım */
  selectorTitleEmphasis?: string;
  /** Seçici başlık son kısım */
  selectorTitleSuffix?: string;
  /** Seçici sağ yan zengin metin */
  selectorSideHtml?: string;
  /** Kart link metni (İncele) */
  selectorCardCtaText?: string;
  /** Bölüm numarası (02) */
  featureNumber?: string;
  /** Bölüm etiketi */
  featureLabel?: string;
  /** Öne çıkan blok rozet metni */
  featureEyebrow?: string;
  /** Öne çıkan kart başlık ilk kısım */
  featureTitlePrefix?: string;
  /** Öne çıkan kart vurgulu başlık */
  featureTitleEmphasis?: string;
  /** Öne çıkan kart başlık son kısım */
  featureTitleSuffix?: string;
  /** Öne çıkan kart açıklama */
  featureDescriptionHtml?: string;
  /** Öne çıkan kart buton linki */
  featureHref?: string;
  /** Öne çıkan kart buton metni */
  featureCtaText?: string;
  /** 1. teknik özellik adı */
  featureSpec1Label?: string;
  /** 1. teknik özellik değeri */
  featureSpec1Value?: string;
  /** 2. teknik özellik adı */
  featureSpec2Label?: string;
  /** 2. teknik özellik değeri */
  featureSpec2Value?: string;
  /** 3. teknik özellik adı */
  featureSpec3Label?: string;
  /** 3. teknik özellik değeri */
  featureSpec3Value?: string;
  /** 4. teknik özellik adı */
  featureSpec4Label?: string;
  /** 4. teknik özellik değeri */
  featureSpec4Value?: string;
  /** Bölüm numarası (03) */
  detailNumber?: string;
  /** Bölüm etiketi */
  detailLabel?: string;
  /** Detay başlık ilk kısım */
  detailTitlePrefix?: string;
  /** Detay vurgulu başlık */
  detailTitleEmphasis?: string;
  /** Detay başlık son kısım */
  detailTitleSuffix?: string;
  /** Detay sağ yan açıklama metni */
  detailSideHtml?: string;
  /** 1. segment / neden kartı başlığı */
  detailCard1Title?: string;
  /** 1. kart açıklama metni */
  detailCard1DescriptionHtml?: string;
  /** 2. segment / neden kartı başlığı */
  detailCard2Title?: string;
  /** 2. kart açıklama metni */
  detailCard2DescriptionHtml?: string;
  /** 3. segment / neden kartı başlığı */
  detailCard3Title?: string;
  /** 3. kart açıklama metni */
  detailCard3DescriptionHtml?: string;
  /** 4. segment / neden kartı başlığı */
  detailCard4Title?: string;
  /** 4. kart açıklama metni */
  detailCard4DescriptionHtml?: string;
  /** Alt çağrı kutusu başlık */
  detailCalloutTitlePrefix?: string;
  /** Alt çağrı kutusu vurgulu başlık */
  detailCalloutTitleEmphasis?: string;
  /** Alt çağrı kutusu son başlık */
  detailCalloutTitleSuffix?: string;
  /** Alt çağrı kutusu açıklama metni */
  detailCalloutDescriptionHtml?: string;
  /** 1. buton metni */
  detailCalloutButton1Text?: string;
  /** 1. buton linki */
  detailCalloutButton1Href?: string;
  /** 2. buton metni */
  detailCalloutButton2Text?: string;
  /** 2. buton linki */
  detailCalloutButton2Href?: string;
  /** Bölüm sıra numarası (04) */
  faqNumber?: string;
  /** Bölüm başlık etiketi */
  faqLabel?: string;
  /** SSS bölümü ana başlığı */
  faqTitle?: string;
  /** SSS başlığının yanındaki açıklama */
  faqSideHtml?: string;
  /** 1. sıkça sorulan soru başlığı */
  faq1Question?: string;
  /** 1. soru için zengin metin cevabı */
  faq1AnswerHtml?: string;
  /** 2. sıkça sorulan soru başlığı */
  faq2Question?: string;
  /** 2. soru için zengin metin cevabı */
  faq2AnswerHtml?: string;
  /** 3. sıkça sorulan soru başlığı */
  faq3Question?: string;
  /** 3. soru için zengin metin cevabı */
  faq3AnswerHtml?: string;
  /** 4. sıkça sorulan soru başlığı */
  faq4Question?: string;
  /** 4. soru için zengin metin cevabı */
  faq4AnswerHtml?: string;
  /** 5. sıkça sorulan soru başlığı */
  faq5Question?: string;
  /** 5. soru için zengin metin cevabı */
  faq5AnswerHtml?: string;
  /** Kapanış başlığı ilk kısım */
  finalTitlePrefix?: string;
  /** Kapanış başlığı vurgulu kısım */
  finalTitleEmphasis?: string;
  /** Kapanış başlığı son kısım */
  finalTitleSuffix?: string;
  /** Kapanış açıklama metni */
  finalDescriptionHtml?: string;
  /** Kapanış ana buton metni */
  finalPrimaryButtonText?: string;
  /** Kapanış ana buton linki */
  finalPrimaryButtonHref?: string;
  /** Kapanış ikincil buton metni */
  finalSecondaryButtonText?: string;
  /** Kapanış ikincil buton linki */
  finalSecondaryButtonHref?: string;
  /** Sayfa arka plan rengi */
  backgroundColor?: string;
  /** Sayfa metin rengi */
  textColor?: string;
  /** İkincil metin ve etiket rengi */
  mutedTextColor?: string;
  /** Kart arka plan rengi */
  panelColor?: string;
  /** Buton ve rozet vurgu rengi (Lime) */
  accentColor?: string;
  /** Bölüm ve kart kenarlık rengi */
  lineColor?: string;
}
