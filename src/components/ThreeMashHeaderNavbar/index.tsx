import { HeaderNavbarPart } from "../ThreeMashHeader";
import type { Props } from "./types";

export function ThreeMashHeaderNavbar(props: Props) {
  return <HeaderNavbarPart {...props} />;
}

export default ThreeMashHeaderNavbar;
