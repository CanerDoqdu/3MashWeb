diff --git a/dist/commands/build.js b/dist/commands/build.js
index a42b8002e43092861ba0990d9982e27c179299f5..3ef76ed84bb87dcec4d602b65a88f3521977b2dd 100644
--- a/dist/commands/build.js
+++ b/dist/commands/build.js
@@ -65,7 +65,8 @@ async function buildComponents(options) {
             const metadata = {
                 id: component.id,
                 name: component.name,
-                props: component.props
+                props: component.props,
+                ...(component.type ? { type: component.type } : {})
             };
             fs.writeFileSync(path.join(componentOutDir, "metadata.json"), JSON.stringify(metadata, null, 2));
             spinner.succeed(chalk.green(`${component.name} built successfully`));
@@ -86,7 +87,7 @@ async function buildComponents(options) {
             id: c.id,
             name: c.name,
             props: c.props,
-            ...(c.type && c.type !== "component" ? { type: c.type } : {}),
+            ...(c.type ? { type: c.type } : {}),
         }))
     };
     fs.writeFileSync(path.join(outputPath, "manifest.json"), JSON.stringify(manifest, null, 2));
diff --git a/dist/commands/config.js b/dist/commands/config.js
index 4d7821ab8f19c9e876c06bdd104dd3d72eff34f6..442846762d1d9d8689d327b6b4a4e56b173a82e8 100644
--- a/dist/commands/config.js
+++ b/dist/commands/config.js
@@ -617,9 +617,11 @@ function parseDefaultValue(value, propType) {
             return value === "true";
         case "LINK":
         case "LIST_OF_LINK":
+        case "COMPONENT":
+        case "COMPONENT_LIST":
             // The --default-value flag arrives as a string; LINK values must be stored as a
-            // typed object. Parse it here; if it is not valid JSON, return the raw string so
-            // validateLinkDefaultValue can report a precise error.
+            // typed object and component slots must preserve their nested component tree.
+            // Parse JSON-backed values here; validators can still report malformed links.
             try {
                 return JSON.parse(value);
             }
diff --git a/dist/commands/publish.js b/dist/commands/publish.js
index 9490605e24efd27d4549bfdf79acedb7863e3b49..23d266c0ac04c94d8cea814f1190a8d399107b09 100644
--- a/dist/commands/publish.js
+++ b/dist/commands/publish.js
@@ -174,6 +174,9 @@ async function publishComponents(options) {
             typeId: prop.typeId,
             enumTypeId: prop.enumTypeId,
             ...(prop.privateVarMap ? { privateVarMap: prop.privateVarMap } : {}),
+            ...(prop.filteredComponentIds
+                ? { filteredComponentIds: prop.filteredComponentIds }
+                : {}),
         }));
         // Build prop group hierarchy if groups are defined
         const propGroups = component.propGroups && component.propGroups.length > 0
@@ -200,9 +203,7 @@ async function publishComponents(options) {
             canvasClientJs,
             sourceType: "external",
             lastCompiledAt: Date.now(),
-            ...(component.type && component.type !== "component"
-                ? { componentType: component.type }
-                : {}),
+            ...(component.type ? { componentType: component.type } : {}),
             ...(component.isHeader ? { isHeader: true } : {}),
             ...(component.isFooter ? { isFooter: true } : {}),
             ...(propGroups ? { propGroups } : {}),
diff --git a/dist/utils/esm-transform.js b/dist/utils/esm-transform.js
index 361e46364b4eab384e112bffd08e4ae8ca7ce5a3..38d336371424273e9f89f759ae30379e68923179 100644
--- a/dist/utils/esm-transform.js
+++ b/dist/utils/esm-transform.js
@@ -173,7 +173,10 @@ export function transformEsmToWindowGlobals(code, options) {
         return `/* stripped: ${match} */`;
     });
     // Strip any remaining bare `import "module"` side-effect imports
-    transformed = transformed.replace(/import\s*["'][^"']+["']\s*;?/g, (match) => {
+    transformed = transformed.replace(/import\s*["']([^"']+)["']\s*;?/g, (match, moduleName) => {
+        if (chunkMapping && isChunkImport(moduleName)) {
+            return `/* stripped: ${match} */`;
+        }
         console.warn(`[esm-transform] Side-effect import stripped: ${match}`);
         return `/* stripped: ${match} */`;
     });
diff --git a/dist/utils/forbid-external-packages.js b/dist/utils/forbid-external-packages.js
index a2c802f93f710d3f72ac91ee73c9cbbd403cc3bc..b97d8e6f6211ec2baf95be10600a8efb11cd1e8a 100644
--- a/dist/utils/forbid-external-packages.js
+++ b/dist/utils/forbid-external-packages.js
@@ -46,6 +46,9 @@ function packageRootOf(specifier) {
     }
     return specifier.split("/")[0];
 }
+function isWindowsAbsolutePath(specifier) {
+    return /^[A-Za-z]:[\\/]/.test(specifier) || specifier.startsWith("\\\\");
+}
 /**
  * Fail the build when a code component imports an npm package that is not on the allowlist.
  *
@@ -66,6 +69,8 @@ export function forbidExternalPackagesPlugin() {
             // Bare specifiers only — skip relative ("./", "../") and absolute ("/") imports.
             build.onResolve({ filter: /^[^./]/ }, (args) => {
                 const specifier = args.path;
+                if (isWindowsAbsolutePath(specifier))
+                    return undefined;
                 if (NODE_BUILTINS.has(specifier))
                     return undefined;
                 const root = packageRootOf(specifier);
